export interface faceOptions {
    /**
     * 默认尺寸，css单位，默认3.5em
     */
    defaultSize?: string
    /**
     * 后缀名，默认 png
     */
    defaultSuffix?: string
    /**
     * 表情包和表情名之前的路径，默认`/img/face/`
     */
    path?: string
  }